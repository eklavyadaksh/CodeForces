#include<iostream>
#include <vector>
#include<set>
using namespace std;
int main()
{
  int a,b,c,d;
  cin>>a>>b>>c>>d;
vector<int> colors={a,b,c,d};
 
set<int> s;
s.insert(colors.begin(),colors.end());
cout<<colors.size()-s.size();
 
return 0;
}